# Handoffs

Store versioned handoff summaries here.

Naming convention:

`HANDOFF-<sequence>-<task-slug>-v<version>.md`

Example:

`HANDOFF-0001-initialize-coordination-repository-v001.md`

Do not overwrite an existing version. Create a new numbered or versioned artifact instead.

