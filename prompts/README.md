# Prompts

Store versioned ChatGPT-to-Codex prompts here.

Naming convention:

`PROMPT-<sequence>-<task-slug>-v<version>.json`

Example:

`PROMPT-0001-initialize-coordination-repository-v001.json`

Do not overwrite an existing version. Create a new numbered or versioned artifact instead.

