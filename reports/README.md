# Reports

Store versioned Codex task reports here.

Naming convention:

`REPORT-<sequence>-<task-slug>-v<version>.json`

Example:

`REPORT-0001-initialize-coordination-repository-v001.json`

Do not overwrite an existing version. Create a new numbered or versioned artifact instead.

